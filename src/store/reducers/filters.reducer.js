const initialState = {
  text: "",
  sortBy: "date",
  startDate: null,
  endDate: null
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
