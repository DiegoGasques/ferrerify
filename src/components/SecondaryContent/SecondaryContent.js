import React from "react";
import "./SecondaryContent.scss";

export const SecondaryContent = ({ children }) => {
  return <div className="SecondaryContent">{children}</div>;
};

export default SecondaryContent;
